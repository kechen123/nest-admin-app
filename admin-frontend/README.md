# Vue 3 企业级管理后台系统

一个现代化、优雅的企业级管理后台系统，注重用户体验、视觉美感和功能完整性。

## ✨ 技术栈

- **前端框架**: Vue 3 + TypeScript
- **UI框架**: Element Plus
- **状态管理**: Pinia
- **路由**: Vue Router
- **构建工具**: Vite
- **样式方案**: SCSS + CSS Modules
- **HTTP客户端**: Axios

## 🚀 快速开始

### 1. 安装依赖

```bash
npm install
# 或
pnpm install
```

### 2. 配置环境变量

复制 `.env.example` 为 `.env` 并修改配置：

```bash
cp .env.example .env
```

### 3. 运行项目

开发模式：

```bash
npm run dev
```

构建生产版本：

```bash
npm run build
```

### 4. 访问应用

启动后访问：http://localhost:5173

## 📁 项目结构

```
src/
├── api/            # API 请求模块
│   ├── request.ts  # Axios 封装
│   ├── auth.ts     # 认证相关接口
│   └── user.ts     # 用户相关接口
├── assets/         # 静态资源
├── components/     # 通用组件
│   ├── Breadcrumb/      # 面包屑导航
│   ├── DataCard/        # 数据卡片
│   ├── DataPreview/     # 数据预览
│   ├── PermissionButton/# 权限按钮
│   └── SkeletonTable/   # 骨架屏表格
├── hooks/          # 自定义 Hooks
│   ├── usePermission.ts # 权限相关
│   └── useTable.ts      # 表格相关
├── layouts/        # 布局组件
│   └── MainLayout.vue   # 主布局
├── router/         # 路由配置
├── stores/         # Pinia 状态管理
│   ├── app.ts      # 应用全局状态
│   ├── permission.ts # 权限状态
│   └── user.ts     # 用户状态
├── styles/         # 全局样式
│   ├── index.scss      # 样式入口
│   ├── variables.scss  # 变量定义
│   ├── mixins.scss     # Mixins
│   ├── animations.scss # 动画定义
│   ├── theme.scss      # 主题样式
│   └── reset.scss      # 重置样式
├── utils/          # 工具函数
│   ├── auth.ts     # 权限工具
│   ├── format.ts   # 格式化工具
│   ├── validate.ts # 验证工具
│   ├── storage.ts  # 存储工具
│   └── common.ts   # 通用工具
└── views/          # 页面视图
    ├── dashboard/  # 仪表盘
    ├── login/      # 登录页
    ├── user/       # 用户管理
    ├── settings/   # 系统设置
    └── profile/    # 个人中心
```

## 🎨 核心功能

### 1. 认证与权限系统
- ✅ JWT Token 认证
- ✅ RBAC 权限控制
- ✅ 动态路由加载
- ✅ 按钮级权限控制 (`PermissionButton` 组件)

### 2. 仪表盘 Dashboard
- ✅ 数据概览卡片（支持数字格式化）
- ✅ 实时数据图表（预留接口）
- ✅ 快捷操作入口
- ✅ 最近活动记录

### 3. 数据管理 CRUD
- ✅ 智能表格组件（带虚拟滚动）
- ✅ 高级筛选器
- ✅ 批量操作
- ✅ 数据预览模态框
- ✅ 骨架屏加载状态

### 4. 系统设置
- ✅ 主题切换（明亮/暗黑模式）
- ✅ 布局配置
- ✅ 个人中心
- ✅ 密码修改

### 5. 交互优化
- ✅ 页面切换过渡动画
- ✅ 列表项悬停效果
- ✅ 按钮点击反馈
- ✅ 数据更新平滑过渡
- ✅ 骨架屏加载体验

## 🎯 功能特性

### 权限系统
使用 `PermissionButton` 组件实现按钮级权限控制：

```vue
<PermissionButton
  type="primary"
  permission="user:create"
  @click="handleCreate"
>
  新增用户
</PermissionButton>
```

### 主题切换
支持明亮和暗黑两种主题模式，自动保存用户偏好：

```typescript
import { useAppStore } from '@/stores/app';

const appStore = useAppStore();
appStore.setTheme('dark'); // 切换到暗黑模式
```

### 表格 Hook
使用 `useTable` Hook 简化表格数据管理：

```typescript
const { loading, tableData, pagination, refresh } = useTable({
  fetchData: async (params) => {
    const response = await userApi.getUserList(params);
    return {
      list: response.list,
      total: response.total,
    };
  },
});
```

### 工具函数
提供丰富的工具函数：

- `formatDateTime()` - 日期时间格式化
- `formatNumber()` - 数字格式化（千分位）
- `hasPermission()` - 权限检查
- `debounce()` / `throttle()` - 防抖节流
- `deepClone()` - 深拷贝

## 📝 开发说明

### API 请求
统一使用 `src/api/request.ts` 封装的实例，自动处理：
- Token 添加
- 错误提示
- 401 自动跳转登录

### 状态管理
- **用户状态**: `src/stores/user.ts`
- **应用状态**: `src/stores/app.ts` (主题、布局等)
- **权限状态**: `src/stores/permission.ts`

### 路由守卫
路由守卫在 `src/router/index.ts` 中配置，自动检查登录状态和权限。

### 样式系统
- 使用 SCSS 变量统一管理颜色、间距等
- 支持主题切换（明亮/暗黑）
- 提供丰富的动画效果

## 🎨 UI/UX 设计规范

### 配色方案
- 主色: `#409eff`
- 成功: `#67c23a`
- 警告: `#e6a23c`
- 危险: `#f56c6c`

### 设计原则
- **一致性**: 统一的配色、间距、圆角
- **反馈性**: 所有操作应有明确反馈
- **高效性**: 减少用户操作步骤
- **美观性**: 现代简约设计，适当微交互

## 📦 组件说明

### PermissionButton
权限按钮组件，根据权限自动显示/隐藏：

```vue
<PermissionButton
  permission="user:delete"
  role="admin"
  anyPermission="['user:delete', 'user:update']"
>
  删除
</PermissionButton>
```

### DataCard
数据卡片组件，用于展示统计数据：

```vue
<DataCard
  title="总用户数"
  :value="1234"
  :icon="User"
  icon-bg-color="#409eff"
  subtitle="较上月增长 12%"
  format-number
/>
```

### SkeletonTable
骨架屏表格组件，提升加载体验：

```vue
<SkeletonTable :loading="loading" :rows="5" :columns="8">
  <el-table :data="tableData">...</el-table>
</SkeletonTable>
```

## 🔧 环境变量

创建 `.env` 文件：

```env
VITE_API_BASE_URL=http://localhost:3000/api
VITE_APP_TITLE=后台管理系统
```

## 📄 License

MIT
