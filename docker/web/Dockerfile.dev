FROM node:20-alpine

WORKDIR /app

# 复制 package 文件
COPY package*.json ./

# 安装依赖（使用 --legacy-peer-deps 解决依赖冲突）
RUN npm install --legacy-peer-deps

# 复制源代码（会被 volume 覆盖）
COPY . .

# 暴露端口
EXPOSE 4000

# 启动开发服务器
CMD ["npm", "run", "dev"]

