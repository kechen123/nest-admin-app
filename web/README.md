# 管理系统前端

基于 Vue 3 + TypeScript + Vite 构建的现代化管理系统前端应用。

## 📋 目录

- [功能特性](#-功能特性)
- [技术栈](#-技术栈)
- [快速开始](#-快速开始)
- [项目结构](#-项目结构)
- [环境配置](#-环境配置)
- [常用命令](#-常用命令)
- [浏览器支持](#-浏览器支持)
- [常见问题](#-常见问题)

## ✨ 功能特性

- 🎨 **现代化 UI** - 基于 Element Plus 组件库，提供美观的用户界面
- 🔐 **权限管理** - 完整的用户认证和权限控制系统
- 📱 **响应式设计** - 适配各种屏幕尺寸
- 🌙 **主题切换** - 支持明暗主题切换
- 🚀 **性能优化** - 代码分割、懒加载等优化策略
- 📦 **类型安全** - 完整的 TypeScript 类型支持
- 🔄 **自动导入** - 组件和 API 自动导入，提升开发效率
- 📊 **数据可视化** - 集成 ECharts 图表库

## 🛠 技术栈

- **框架**: Vue 3.5+ (Composition API)
- **构建工具**: Vite 6+
- **语言**: TypeScript 5.8+
- **UI 框架**: Element Plus 2.10+
- **状态管理**: Pinia 3.0+
- **路由**: Vue Router 4.5+ (自动路由)
- **HTTP 客户端**: Axios 1.9+
- **图表库**: ECharts 5.6+
- **样式**: Less 4.3+
- **代码规范**: ESLint + Prettier

## 🚀 快速开始

### 环境要求

- Node.js >= 18.0.0
- pnpm >= 8.0.0 (推荐) 或 npm >= 9.0.0

### 安装依赖

```bash
# 使用 pnpm (推荐)
pnpm install

# 或使用 npm
npm install
```

### 启动开发服务器

```bash
# 使用 pnpm
pnpm dev

# 或使用 npm
npm run dev
```

启动成功后，访问 [http://localhost:4000](http://localhost:4000)

### 构建生产版本

```bash
# 使用 pnpm
pnpm build

# 或使用 npm
npm run build
```

构建产物将输出到 `dist` 目录。

### 预览生产构建

```bash
# 使用 pnpm
pnpm preview

# 或使用 npm
npm run preview
```

## 📁 项目结构

```text
web/
├── public/                 # 静态资源目录
├── src/
│   ├── api/               # API 接口定义
│   ├── assets/            # 资源文件（图片、样式等）
│   ├── components/        # 公共组件
│   │   ├── Kc/           # 自定义业务组件库
│   │   ├── Layout/       # 布局组件
│   │   └── ...
│   ├── directives/        # 自定义指令
│   ├── hooks/            # 组合式函数
│   ├── layouts/          # 布局模板
│   ├── modules/          # 功能模块
│   │   ├── router/       # 路由配置
│   │   ├── pinia/        # 状态管理
│   │   └── ...
│   ├── pages/            # 页面组件（自动路由）
│   ├── stores/           # Pinia 状态存储
│   ├── utils/            # 工具函数
│   │   ├── http/         # HTTP 请求封装
│   │   └── ...
│   ├── App.vue           # 根组件
│   └── main.ts           # 入口文件
├── types/                # TypeScript 类型定义
├── index.html            # HTML 模板
├── vite.config.ts        # Vite 配置
├── tsconfig.json         # TypeScript 配置
└── package.json          # 项目配置
```

## ⚙️ 环境配置

### 开发环境

开发环境默认配置：

- 开发服务器端口: `4000`
- API 代理: `/api` -> `http://localhost:3000`
- 自动打开浏览器: `false`

### 环境变量

创建 `.env` 文件（可选）：

```env
# API 基础路径（生产环境）
VITE_APP_BASE_API=/api

# 其他环境变量...
```

## 📝 常用命令

```bash
# 开发
pnpm dev                  # 启动开发服务器

# 构建
pnpm build                # 构建生产版本
pnpm type-check           # 类型检查
pnpm build-only           # 仅构建（不进行类型检查）

# 代码质量
pnpm lint                 # 运行所有 linter
pnpm lint:eslint          # 运行 ESLint
pnpm lint:oxlint          # 运行 Oxlint
pnpm format               # 格式化代码

# 预览
pnpm preview              # 预览生产构建
```

## 🌐 浏览器支持

- Chrome (最新版)
- Firefox (最新版)
- Safari (最新版)
- Edge (最新版)

## ❓ 常见问题

### 1. 端口被占用

如果 4000 端口被占用，可以修改 `vite.config.ts` 中的 `server.port` 配置。

### 2. API 请求失败

确保后端服务已启动，并且运行在 `http://localhost:3000`。如果需要修改后端地址，请更新 `vite.config.ts` 中的 `proxy` 配置。

### 3. 依赖安装失败

- 确保 Node.js 版本 >= 18.0.0
- 尝试清除缓存后重新安装：

  ```bash
  rm -rf node_modules pnpm-lock.yaml
  pnpm install
  ```

### 4. 类型错误

运行类型检查：

```bash
pnpm type-check
```

### 5. 路由不生效

确保页面文件放在 `src/pages/` 目录下，文件名遵循 Vue Router 自动路由规则。

## 📚 相关文档

- [开发文档](./DEVELOPMENT.md) - 详细的开发指南
- [Vue 3 文档](https://cn.vuejs.org/)
- [Vite 文档](https://cn.vitejs.dev/)
- [Element Plus 文档](https://element-plus.org/zh-CN/)
- [Vue Router 文档](https://router.vuejs.org/zh/)

## 📄 许可证

[在此添加许可证信息]
