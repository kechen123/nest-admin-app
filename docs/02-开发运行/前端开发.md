# 前端开发

> 📖 **文档导航**：[返回首页](../../README.md) | [快速开始](./快速开始.md)  
> 📚 **详细文档**：[web/README.md](../../web/README.md) | [web/DEVELOPMENT.md](../../web/DEVELOPMENT.md)

## 快速开始

### 安装依赖

```bash
cd web
pnpm install  # 或 npm install
```

### 启动开发服务器

```bash
pnpm dev  # 或 npm run dev
```

启动成功后，访问 [http://localhost:4000](http://localhost:4000)

## 技术栈

- **Vue 3.5+** - 渐进式 JavaScript 框架（使用 Composition API）
- **TypeScript 5.8+** - 类型安全的 JavaScript
- **Vite 6+** - 下一代前端构建工具
- **Element Plus 2.10+** - 基于 Vue 3 的组件库
- **Pinia 3.0+** - Vue 的状态管理库
- **Vue Router 4.5+** - Vue.js 官方路由管理器（自动路由）

## 项目结构

```
web/
├── src/
│   ├── api/               # API 接口定义
│   ├── assets/            # 静态资源
│   ├── components/        # 公共组件
│   ├── hooks/             # 组合式函数
│   ├── layouts/           # 布局模板
│   ├── modules/           # 功能模块
│   ├── pages/             # 页面组件（自动路由）
│   ├── stores/            # Pinia 状态存储
│   └── utils/              # 工具函数
├── public/                 # 静态资源目录
└── package.json           # 项目配置
```

## 常用命令

```bash
# 开发
pnpm dev                  # 启动开发服务器

# 构建
pnpm build                # 构建生产版本
pnpm type-check           # 类型检查

# 代码质量
pnpm lint                 # 运行所有 linter
pnpm format               # 格式化代码

# 预览
pnpm preview              # 预览生产构建
```

## 核心功能

### 自动导入

项目配置了自动导入，以下内容无需手动导入：

- **Vue API**: `ref`, `computed`, `watch` 等
- **Vue Router**: `useRouter`, `useRoute` 等
- **Pinia**: `defineStore`, `storeToRefs` 等
- **Element Plus**: 组件和图标

### 路由系统

使用 `unplugin-vue-router` 实现基于文件系统的自动路由：

- 文件路径 = 路由路径
- `_detail.vue` 表示详情页（动态路由）
- `[...notFond].vue` 表示 404 页面

### 权限控制

1. **路由权限**: 在路由守卫中检查权限
2. **按钮权限**: 使用 `PermissionButton` 组件
3. **API 权限**: 后端接口控制

## 相关文档

- [web/README.md](../../web/README.md) - 前端项目说明
- [web/DEVELOPMENT.md](../../web/DEVELOPMENT.md) - 详细开发指南
- [快速开始](./快速开始.md)

