# 镜像导出

## Windows 系统导出镜像

### PowerShell 方式

```powershell
# 查看镜像名称
docker images

# 导出所有镜像到一个文件（推荐）
docker save yl-project_backend:latest yl-project_web:latest -o "$env:USERPROFILE\Desktop\yl-images.tar"

# 或分别导出
docker save -o "$env:USERPROFILE\Desktop\yl-backend.tar" yl-project_backend:latest
docker save -o "$env:USERPROFILE\Desktop\yl-web.tar" yl-project_web:latest
```

### CMD 方式

```cmd
docker save yl-project_backend:latest yl-project_web:latest -o %USERPROFILE%\Desktop\yl-images.tar
```

## Linux/Mac 系统导出镜像

```bash
# 导出所有镜像到一个文件
docker save yl-project_backend:latest yl-project_web:latest -o ~/Desktop/yl-images.tar

# 或分别导出
docker save -o ~/Desktop/yl-backend.tar yl-project_backend:latest
docker save -o ~/Desktop/yl-web.tar yl-project_web:latest
```

## 验证导出文件

### Windows PowerShell

```powershell
# 检查文件是否存在
Test-Path "$env:USERPROFILE\Desktop\yl-images.tar"

# 查看文件大小
Get-Item "$env:USERPROFILE\Desktop\yl-images.tar" | Select-Object Name, @{Name="Size(MB)";Expression={[math]::Round($_.Length/1MB, 2)}}
```

### Linux/Mac

```bash
ls -lh ~/Desktop/yl-images.tar
```

## 注意事项

1. **镜像名称**：如果 `docker images` 显示的镜像名不同，请替换命令中的名称
2. **文件大小**：镜像文件可能很大（几百 MB 到几 GB），确保桌面有足够空间
3. **导出时间**：可能需要几分钟，请耐心等待
4. **桌面路径**：`$env:USERPROFILE\Desktop` 是标准桌面路径，如果桌面位置不同，请使用实际路径

## 压缩镜像文件（可选）

如果镜像文件太大，可以压缩后传输：

```bash
# Windows PowerShell
Compress-Archive -Path "$env:USERPROFILE\Desktop\yl-images.tar" -DestinationPath "$env:USERPROFILE\Desktop\yl-images.tar.gz"

# Linux/Mac
tar -czf ~/Desktop/yl-images.tar.gz ~/Desktop/yl-images.tar
```

## 在服务器上加载镜像

```bash
# SSH 登录服务器
ssh user@your-server-ip

# 加载镜像
docker load -i yl-images.tar

# 验证镜像加载成功
docker images
```

## 📚 相关文档

- [构建指南](./构建指南.md) - 生产镜像构建
- [部署指南](./部署指南.md) - 服务器部署流程

